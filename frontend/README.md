# Smart Expense Tracker - Frontend

This is a Vite + React + Tailwind frontend for the Smart Expense Tracker backend.

Install:

```bash
cd frontend
npm install
```

Run (development):

```bash
npm run dev
```

Default dev server: http://localhost:3000

Adjust backend URL in `src/services/api.js` if needed.
